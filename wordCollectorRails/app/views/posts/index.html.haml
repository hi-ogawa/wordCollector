%h1 Listing Uncategorized Words

Move the selected words into the category
= select_tag("category", options_from_collection_for_select(@categories, "id", "name"))
%button{:onclick => "changeCat()"} go!

%table
  %thead
    %tr
      %th Word
      %th Sentence
      %th{:colspan => "1"}
  %tbody.words
    - @posts.each do |post|
      %tr
        %td.word{:pic_data => post.picture, :post_id => post.id}= post.word
        %td.sentence= post.sentence
        %td.misc
          = link_to 'Edit', edit_post_path(post)
          = link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' }

%br

-# = link_to 'New Post', new_post_path
= link_to 'See Categories', '/categories/'
