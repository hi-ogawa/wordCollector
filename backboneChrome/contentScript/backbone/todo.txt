-# adopt https <- calling ajax from eventPage
- desgin by bootstrap
  -# popover properly
  - show/hide setting
-# popover ui: mouseover, mouseleave

-# listen clicks on body content
  -# take in words, sentences

-# take tab screenshot
-# category set
  -# storage (not by localstorage, but by chrome.storage since localstorage won't share data beyond different webpage (probably, domain?))

- login/logout should be achieved in popup

- animation

-# insertJS should happen only once when popup is pressed at the first time
